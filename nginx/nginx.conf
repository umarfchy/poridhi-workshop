events {
    # empty placeholder
}


http {

    server {
        listen 80;

        location / {
            proxy_pass http://frontend;
        }

        location /api {
            proxy_pass http://backend;
        }
    }

    upstream frontend {
        server client-service;
    }

    upstream backend {
        server api-service;
    }
}